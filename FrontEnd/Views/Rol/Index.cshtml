@model List<FrontEnd.Models.RolViewModel>

@{
    ViewData["Title"] = "Lista de Roles";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<!-- Page Content -->
<section id="page-content" class="no-sidebar">
    <div class="container">
        <!-- DataTable -->
        <div class="row mb-5">
            <div class="col-lg-12">
                <h4>Lista de Roles</h4>
                <table id="datatable" class="table table-bordered table-hover" style="width:100%">
                    <thead>
                        <tr>
                            <th>Nombre del Rol</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var rol in Model)
                        {
                            <tr>
                                <td>@rol.NombreRol</td>
                                <td>
                                    <a class="ml-2" asp-controller="Rol" asp-action="Edit" asp-route-id="@rol.Id" data-toggle="tooltip" data-original-title="Edit"><i class="icon-edit"></i></a>
                                    <!-- Botón para activar el modal -->
                                    <a class="ml-2" href="#" data-toggle="modal" data-target="#deleteModal-@rol.Id">
                                        <i class="icon-trash-2"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- end: DataTable -->
        <!-- Add Role Button -->
        <div class="row">
            <div class="col-lg-12">
                <a asp-controller="Rol" asp-action="Create" class="btn btn-primary">Agregar Rol</a>
            </div>
        </div>
    </div>
</section>
<!-- end: Page Content -->
<!-- Modal -->
@foreach (var rol in Model)
{
    <div class="modal fade" id="deleteModal-@rol.Id" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel-@rol.Id">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteModalLabel-@rol.Id">Confirmar Eliminación</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Estás seguro de que quieres eliminar el rol @rol.NombreRol?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <!-- Botón de confirmación que enviará el formulario -->
                    <button type="submit" form="deleteForm-@rol.Id" class="btn btn-danger">Eliminar</button>
                </div>
            </div>
        </div>
    </div>
    <!-- Formulario de eliminación para cada rol -->
    <form asp-action="Delete" asp-controller="Rol" asp-route-id="@rol.Id" method="post" id="deleteForm-@rol.Id">
        @Html.AntiForgeryToken()
    </form>
}
